<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<title>Grocery List</title>
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.2/jquery.min.js"></script>
		<link href="/css/style.css" rel="stylesheet" type="text/css">
	</head>
	<body>
		<header>
			<h1>Grocery List</h1>
			<h2>Author: Quentin Nolan</h2>
		</header>
		<ul class="items">
			<li>
				<form action="/items" method="POST">
					<input type="number" min="0" max="50" value="1" class="item_nombre" name="nombre">
					<input type="text" placeholder="What's my name" minlength="1" maxlength="50" class="item_aliment" name="aliment">
					<button type="submit" class="add_item">Add</button>
				</form>
			</li>
			<% for(var i=0; i<items.length; i++) {%>
			<li class="item" data="<%= items[i]._id %>">
				<input type="number" value="<%= items[i].nombre %>" min="0" max="50" class="item_nombre" name="nombre">
				<input type="text" value="<%= items[i].aliment %>" maxlength="50" class="item_aliment" name="aliment">
				<button class="update_item">Edit</button>
				<button class="delete_item">Delete</button>
			</li>
			<% } %>
		</ul>

		<script src="main.js"></script>
		<footer>
			<a href="https://ca.linkedin.com/in/nolanquentin">Who am I?</a>
		</footer>
	</body>
</html>